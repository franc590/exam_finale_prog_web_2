<h1>Ajouter volume</h1>
<form action="/volume/ajouter" method="POST" enctype="multipart/form-data">
    <p>
        titre: 
        <input type="text" name="login" id="login" 
        placeholder="Entrez le titre"
        value="<%= typeof titre != 'undefined' ? titre : '' %>"
        >
    </p>
    <p>
        NomImage: 
        <input type="text" name="nom" id="nom"
        placeholder="Entrez l'image"
        value="<%= typeof nomImage != 'undefined' ? nomImage : '' %>"
        ></p>
    <p>